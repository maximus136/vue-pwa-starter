/*
  It is a good practice to store the mutation names in a separate object and use each of them as computed property keys when defining the mutation.
*/

export const PAGE_STATUS = 'updatePageStatus';
